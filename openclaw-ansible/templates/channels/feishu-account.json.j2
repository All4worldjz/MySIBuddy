{
  "account": "{{ item.account }}",
  "appIdRef": "{{ item.app_id_ref }}",
  "appSecretRef": "{{ item.app_secret_ref }}",
  "agent": "{{ item.agent }}",
  "enabled": true,
  "plugin": "openclaw-lark"
}
