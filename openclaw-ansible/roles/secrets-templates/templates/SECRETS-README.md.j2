# OpenClaw 密钥配置说明

## ⚠️ 重要提示

本文件包含敏感信息，请妥善保管！

## 配置步骤

### 1. 编辑 runtime-secrets.json

```bash
vim {{ openclaw_config_dir }}/runtime-secrets.json
```

填入以下真实密钥：

| 变量名 | 说明 | 获取方式 |
|--------|------|----------|
| `MINIMAX_API_KEY` | MiniMax API 密钥 | 从 MiniMax 控制台获取 |
| `MODELSTUDIO_API_KEY` | 阿里云百炼 API 密钥 | 从阿里云百炼控制台获取 |
| `TELEGRAM_CHIEF_BOT_TOKEN` | Telegram Chief Bot Token | 从 @BotFather 获取 |
| `TELEGRAM_PERSONAL_BOT_TOKEN` | Telegram Personal Bot Token | 从 @BotFather 获取 |
| `TELEGRAM_MENTOR_BOT_TOKEN` | Telegram Mentor Bot Token | 从 @BotFather 获取 |
| `FEISHU_WORK_APP_ID` | 飞书 Work App ID | 从飞书开放平台获取 |
| `FEISHU_WORK_APP_SECRET` | 飞书 Work App Secret | 从飞书开放平台获取 |
| `FEISHU_SCRIBE_APP_ID` | 飞书 Scribe App ID | 从飞书开放平台获取 |
| `FEISHU_SCRIBE_APP_SECRET` | 飞书 Scribe App Secret | 从飞书开放平台获取 |
| `OPENCLAW_GATEWAY_TOKEN` | OpenClaw Gateway 认证令牌 | 自行生成（随机字符串） |

### 2. 编辑 gateway.env

```bash
vim {{ openclaw_config_dir }}/gateway.env
```

填入与 runtime-secrets.json 相同的密钥值。

### 3. 设置文件权限

```bash
chmod 600 {{ openclaw_config_dir }}/runtime-secrets.json
chmod 600 {{ openclaw_config_dir }}/gateway.env
```

### 4. 重启 Gateway

```bash
systemctl --user restart {{ openclaw_systemd_service }}
```

### 5. 验证配置

```bash
# 检查密钥加载
openclaw secrets audit

# 检查系统状态
openclaw status --deep

# 检查渠道状态
openclaw status --deep | grep -E "(Channel|ON|OK)"
```

## 故障排除

### Gateway 启动失败

查看日志：
```bash
journalctl --user -u {{ openclaw_systemd_service }} -n 50
```

常见错误：
- `Environment variable "MODELSTUDIO_API_KEY" is missing or empty` → 检查 gateway.env 是否正确配置
- `File secret reference id must be an absolute JSON pointer` → 检查 runtime-secrets.json 中的 id 是否以 `/` 开头

### 渠道显示 OFFLINE

1. 检查 bot token 是否正确
2. 检查防火墙是否允许出站连接
3. 重启 Gateway

## 安全建议

- 定期轮换密钥
- 使用 Ansible Vault 加密存储密钥
- 不要将密钥文件提交到版本控制
- 限制文件访问权限（600）
